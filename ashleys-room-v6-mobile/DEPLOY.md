# Deploy checklist

1. 先完成 `SETUP_SUPABASE.md`。
2. 確認 `config.js` 已填 Supabase URL + Publishable key。
3. 本機測試 Owner Login、儲存、照片、留言。
4. 再部署整個資料夾到 Vercel / Netlify。

重要：不要把 Secret key / service role key 放進 `config.js`。
